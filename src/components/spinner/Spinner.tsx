import * as React from "react";
import { SpinnerContainer, SpinnerBody } from "./style";

const Spinner = () => {
  return (
    <SpinnerContainer>
      <SpinnerBody />
    </SpinnerContainer>
  );
};

export default Spinner;
